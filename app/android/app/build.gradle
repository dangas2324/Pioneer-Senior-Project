buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.0.2' // Make sure the version matches your Flutter version
        classpath 'com.google.gms:google-services:4.3.15'  // For Firebase
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
